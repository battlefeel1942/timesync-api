{
  "name": "time-sync-api",
  "version": "1.0.0",
  "description": "",
  "main": "functions/api.js",
  "scripts": {
    "build": "mkdir -p build && cp -r public/* build/",
    "start": "echo 'No start script needed for Cloudflare Pages'"
  },
  "dependencies": {
    "luxon": "^3.5.0"
  }
}
